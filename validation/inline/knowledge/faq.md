# FAQ

## How do I add a tool?

Create a Python file in the `tools/` directory with public functions.
Each function becomes an MCP tool automatically.

## How do I use S3 as a source?

Set `SOURCE_S3_ENABLED=true` and configure the S3 environment variables.
